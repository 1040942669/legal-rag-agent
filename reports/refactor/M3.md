# M3 PostgreSQL/pgvector 与版本化语料阶段验收报告

> 当前状态：`released_receipt_pending`。截至 2026-09-25，模块 A-D、最终候选与 release target 均已验收；PR #13 已普通合并，annotated `v0.4.0` 与非 draft、非 prerelease GitHub Release 已发布并远端核验。软件发布事实已成立，但独立回执仍需通过自己的 PR、CI、merge 与 master CI，因此本报告尚不把 M3 标为完整 `released`；Issue #12 与 Milestone 4 暂时保持 open，M4 尚未开始。

## 版本和范围

- Milestone / planned version：M3 / `v0.4.0`
- 工作分支：`codex/m3-storage-snapshots`
- 基线：`6cd07d982ffeecec3632ff47c8473118585a9693`，即 M2 最终文档收口后的 `origin/master`
- 模块 A 实现提交：`68f89ed0cd1393d8acda7570a83bb0714f57a868`
- 模块 B 实现提交：`4bc912b09931803d53b4ca3ffdc81d302ba65813`
- 模块 C 实现提交：`1399e18bfca90422829364491ce43d668517d1c4`
- 模块 D 增量提交：
  - `52f865ed0a45ad47c4d1fa1b6d8486ce21e67d96`：激活迁移、rollback 与 re-upgrade 韧性验证
  - `216090e8c9bb68c027a7df83b077d96ebd510f61`：受控实验 HNSW、typed underfill 与 exact fallback
  - `b352c5843dae2f9d0084402417c0ee0014be8ee6`：真实 PostgreSQL 服务重启与新进程验证
  - `cf2c7a82195b63786d77bfb0d5cea9799b18013b`：可复现 import workflow 与 33 项 M3 累计门禁
- 已验证实现 head：`cf2c7a82195b63786d77bfb0d5cea9799b18013b`
- Issue / Milestone：[Issue #12](https://github.com/1040942669/legal-rag-agent/issues/12) / [Milestone 4](https://github.com/1040942669/legal-rag-agent/milestone/4)
- 软件 PR：[PR #13](https://github.com/1040942669/legal-rag-agent/pull/13)，已普通合并
- 最终 PR head：`fff2a4046d04e34664b374553070d9384eeec3c6`
- Release target：`9395c221ea1fc9a9e869b3db04bd76910b77f5b0`
- Tag / Release：annotated `v0.4.0` / [GitHub Release](https://github.com/1040942669/legal-rag-agent/releases/tag/v0.4.0)
- 设计决策：`docs/refactor/decisions/ADR-001-m3-postgresql-pgvector-storage.md`
- 已完成范围：版本化语料与 embedding 导入、数据库不可变约束、exact pgvector 等价、全检索/融合/生成路径的强制边界闭包、artifact 兼容、精确法条目录、revisioned active snapshot、可复现 `plan/dry-run/validate/apply`、显式实验 HNSW、typed underfill/exact fallback、generation-bound build receipt、真实服务重启、`0004` migration、wheel 资源验证和 M0-M3 33 项累计门禁。
- 尚需发布收口：仅剩独立 receipt/finalize 文档 PR、回执 master CI、Issue #12 与 Milestone 4 关闭记录。软件、Tag 与 Release 不再移动，M3 实现范围没有遗留子模块。

## 模块 A-D 实际改动

| 文件/模块 | 机制 | 已验证行为 | 尚未覆盖 |
|---|---|---|---|
| `legal_rag/storage/contracts.py`、`legal_rag/embedding_contracts.py` | immutable DTO、canonical JSON、corpus/bundle/vector hash、完整 profile identity、float32/归一化 contract | snapshot/profile/import 身份可重算；维度、有限数、归一化、revision、关系和 hash 错误失败关闭 | 不判断法律内容的事实正确性 |
| `legal_rag/storage/schema.py`、Alembic `0001`/`0002` | 复合外键、维度 trigger、内容行不可变 trigger、snapshot 状态与成员闭包 trigger | fresh migration 到 `0002_m3_immutable_rows`；已验证语料、关系、向量与导入回执不能被静默改写 | 此行保留模块 A 历史口径；active pointer 与带数据升级的增量见模块 C 行 |
| `legal_rag/storage/repository.py` | 单事务 importer、事务 advisory lock、不可变行比对、导入回执 | 首次导入、完全相同重试、并发重试、冲突拒绝、实际向量回读和后段失败整体回滚 | 当前全局锁牺牲导入并发；多父 snapshot 锁顺序有非阻塞 P2 优化项 |
| `legal_rag/embeddings.py` | `BoundQueryEncoder`、共享向量 canonicalizer、可核验 provider/revision 规则 | query 与 document 使用同一 profile/dimension/normalization contract；不可信 revision 在 encoder 加载和 provider 调用前拒绝 | 真实 provider 调用不属于本模块验收 |
| `legal_rag/retrieval_contracts.py` | frozen typed boundary、typed provenance、boundary fingerprint、top-k contract | scope/snapshot/profile 与可选 law/version/article/article number/effective date 成为一个不可变边界；多 article provenance 逐条严格校验 | fingerprint 只用于 trace；授权判断始终使用完整 typed object |
| `legal_rag/storage/retrieval.py` | exact repository、`PgVectorExactRetriever`、bounded corpus/BM25 wrapper | `<#>` 距离转正 inner-product score；稳定 tie；hard filter 在 `LIMIT` 前执行；读取时重算 payload/vector hash | 不含 ANN，不含法名到 version/article 的产品解析入口 |
| `legal_rag/retrieval.py`、`adaptive.py`、`rerank.py`、`chat.py` | RRF、adaptive/follow-up、rerank、chat 阶段边界复验 | 每一叶子检索、合并、重排、生成和验证前检查 typed provenance；同 chunk ID 不同 payload/provenance 拒绝；chat 使用私有 evidence snapshot | 未绑定的既有离线路径继续兼容，不会被误写成数据库授权路径 |
| `legal_rag/evaluation_artifacts.py`、`chat_artifacts.py` | boundary/provenance 严格序列化、legacy decoder 与 hash-chain 兼容 | 新 artifact 保留完整边界；带边界标记但缺 provenance 的 artifact 失败关闭；既有 M2 unbound artifact 可继续读取 | 不把 legacy artifact 自动升级成可信的 bound artifact |
| `integration_tests/`、`.github/workflows/quality-gate.yml` | 真 PostgreSQL exact/filter/trigger/ANN/migration/restart 测试、digest-pinned pgvector 0.8.6 CI | 最终 PR head 与 release target 均为 61 项 integration；真实 service restart、新进程 verify、wheel `0001` 至 `0004` 资源与 M3 `33/33` gate 均通过 | 不含生产数据库部署、完整真实语料或性能/召回率基准 |
| `legal_rag/storage/catalog.py` | profile-free typed article lookup boundary/request/result、权威 article payload、snapshot membership 与 provenance fingerprint | 精确法名和规范化条号，可选 law identity、version 和/或 effective date；`found / not_found / needs_disambiguation` 互斥；不存在、同名身份、多版本、重叠或未知有效期均失败关闭 | 不做模糊标题、语义搜索或相邻条文兜底；案件应适用哪一版法律不由日期过滤自动决定 |
| `legal_rag/storage/schema.py`、Alembic `0003` | pointer revision/activation ID、不可变 activation ledger、deferred consistency trigger、delimiter-safe legacy bootstrap | 健康 legacy active pointer 的真实 `0002 → 0003` 升级；pointer、snapshot 状态、最新 event 和前驱链提交时一致 | inconsistent legacy 数据按设计拒绝升级；更多负路径和 downgrade/re-upgrade 自动回归留给最终门禁 |
| `PostgresLegalCatalogRepository` | scope 级事务 advisory lock、确定顺序 row lock、完整 snapshot/revision/activation CAS | 首次激活、替换、幂等同目标、回滚和同 scope 双线程竞争；失败保持旧 active，运行中读取固定原 snapshot | deferred trigger 当前扫描全历史，可按 scope 优化；最终 release candidate 仍须累计复验 |
| `legal_rag/storage/import_workflow.py`、`import_cli.py` | 严格本地 manifest、deterministic plan、离线 validate、显式 apply 与 no-clobber machine artifact | plan/dry-run 与 validate 不连接数据库；apply 先重验全部输入，再可选 migrate、事务导入、回读并生成 receipt；任何入口都不会激活快照 | 仓库不提供私人语料或 embedding cache；调用方须自己准备严格 source artifact |
| `legal_rag/storage/ann.py`、Alembic `0004` | profile/dimension/generation 隔离 HNSW、不可变 build receipt、EXPLAIN index proof、typed underfill/exact fallback | exact 保持默认；HNSW 仅显式实验启用；filter 后 underfill、exact 填满、eligible population 不足与 fallback timeout 均有互斥状态 | HNSW 最大 2000 维；未在完整真实法律语料上形成 ANN recall/performance 发布基准 |
| `scripts/m3_restart_probe.py`、`scripts/quality_gate.py` | prepare/restart/new-process verify receipt 与 M0-M3 累计 gate | 真实 PostgreSQL service restart 后复核 migration、数据、active pointer、exact/catalog/ANN 结果与 generation-bound receipt；M3-T01 至 M3-T08 为 33/33 | restart 必须由外部操作者或 CI 真正执行；普通 Engine reconnect 不可冒充服务重启 |

## 模块 B 核心语义

### exact score 与稳定排序

- PostgreSQL 使用 pgvector `<#>` operator。该 operator 返回 negative inner product distance，repository 显式计算 `score = -distance`，输出统一的越大越好 inner-product score，并把 `-0.0` 规范为 `0.0`。
- SQL 首先按 distance 升序，再按 `snapshot_ordinal` 和 `chunk_id` 升序，确保完全并列分数具有稳定次序。RRF、adaptive merge 和 reranker 也使用确定性的次级排序键。
- 同一个 query vector、snapshot、profile 与候选集合下，pgvector exact 的 chunk 顺序和 score 与 NumPy dense 基线在明确浮点容限内一致。

### hard filter 必须先于 `LIMIT`

- 强制边界包含 `scope_id`、`snapshot_id`、`profile_id`，以及可选的 `law_ids`、`version_ids`、`article_ids`、`article_numbers` 和 `[valid_from, valid_to)` `effective_on`。
- exact SQL 在排序和 `LIMIT` 前同时限制 scope、serviceable snapshot、validated import receipt、profile 和法条关系；不会先取全局 top-k 再做应用层过滤。
- BM25 不在全量 corpus 上检索后补过滤，而是先从 PostgreSQL 加载同一个 bounded corpus，再由 `BoundaryBoundRetriever` 绑定完整边界。
- 多 article chunk 采用严格披露语义：至少有一条关系命中且不存在任何违反完整组合 selector 的关系。只要其中一个关联法条越界，整个 chunk 都不会返回，避免混合 chunk 暴露非匹配文本。
- 空 selector tuple 是显式 deny-all；非法 ID、重复 selector、非法日期、非法 `top_k` 和非有限向量在距离查询前失败关闭。

### typed provenance 与下游闭包

- 每个 bound result 携带 `RetrievalProvenance`，包含完整 boundary、scope/snapshot/profile、chunk ID、chunk content hash、重新水合后的 payload hash、snapshot ordinal、embedding hash 和有序 article provenance。
- 数据库关系是 law/version/article metadata 的权威来源。读取时重算持久化 chunk payload 和 float32 vector hash，检测 text/metadata/vector 漂移；migration `0002` 同时阻止受保护行的 update/delete 以及 validated snapshot 的成员追加。
- RRF 要求 lexical/dense 两个叶子同时 unbound，或共享完全相同的 typed boundary；只比较 trace fingerprint 不足以获得信任。融合、adaptive direct/multi-plan/follow-up、rerank、chat staging/generation/verification 均重新校验 provenance。
- 同一 `chunk_id` 若从不同路径带回不同 chunk payload 或 provenance，会直接拒绝，不按分数静默覆盖。chat 在任何 completion 调用前再次核对捕获边界，并对 evidence 做 defensive copy。
- artifact 在保留 M2 unbound 格式和既有 hash chain 可读性的同时，为 bound result 保存完整 typed boundary/provenance；伪造 trace、缺 provenance 或 compatibility metadata 不一致均不能绕过检查。

### query profile 与不可变模型 revision

- retrieval 构造时先核对 snapshot 是否 serviceable、profile 完整 identity 是否与 encoder 相同、snapshot/profile import receipt 是否为 `validated`；构造失败时 encoder 调用数为 0。
- query vector 必须是有限、精确维度的 canonical float32；normalized profile 还要求非零且 L2 norm 在容限内。profile、维度、normalization 或 adapter contract 不一致均在执行相似度查询前报错。
- 在线 version-bound encoder 只接受可核验的 `sentence_transformers` Hub `owner/model` 与 40 位小写 commit SHA revision，不接受本地 repository-shaped 路径，也不允许 `trust_remote_code=True`。当前 SiliconFlow adapter 无法证明不可变 revision，因此不能用于 version-bound query encoder。

## 模块 C 核心语义

### 精确法条目录与显式失败状态

- `ArticleLookupRequest` 使用不含 embedding profile 的独立 boundary：已鉴权调用方固定 `scope_id`、`snapshot_id` 和可选 law/version/article allowlist，查询只依赖关系键和 snapshot membership，不把向量 profile 强加给结构化法条读取。
- 查询键是精确法名与规范化条号，并可选固定 `law_id`、`version_id` 和/或 `effective_on`。SQL 不做模糊标题、语义召回或跨版本 fallback；显式版本不存在或在请求日期不生效时返回 `not_found`，不会换成相邻版本或相邻条文。
- 结果严格分为 `found / not_found / needs_disambiguation`。同名但不同法律身份、无日期的多个精确版本、已核验有效期重叠，以及未知/未核验有效期分别有稳定 reason；候选只披露版本元数据，不携带多个正文。
- 日期统一采用半开区间 `[valid_from, valid_to)`。该条件只证明检索版本元数据覆盖请求日期，不自动判断具体案件应适用的法律版本。
- found 正文来自权威 `law_articles` 行。查询通过 `article → chunk relation → snapshot chunk` 证明成员关系，并重算 article/chunk payload hash；多文章或重复分块只形成有序 membership proof，不拼接混合 chunk 正文。request、boundary、version metadata、article payload 和 membership 均有可重算 fingerprint/hash。

### revisioned active pointer 与原子切换

- active pointer 同时保存 snapshot、单调 revision 与 activation ID，并引用不可变 activation event。event 保存 operation、前一 snapshot/event、目标 snapshot、actor/reason 和事务时间；数据库延迟约束在提交时核对 active 状态、pointer、最新 event 和前驱链。
- activate/replace/rollback 先取得 scope 级 transaction advisory lock，再锁定 pointer 与按 ID 排序的目标/当前 snapshot 行，并对调用方给出的完整 `(snapshot_id, revision, activation_id)` 执行 CAS。同 scope 并发竞争恰好一个事务提交，失败者不留下部分 event、状态或 pointer。
- 新请求从 pointer 读取一次并把 revision/event 固定进 lookup boundary；运行中的 `REPEATABLE READ` 查询在并发切换及旧 snapshot 后续归档时仍读取请求开始时的完整旧快照，下一请求才看到新 active。
- 回滚只允许曾有 activation event、仍为 validated、具备要求 profile 的历史 snapshot。激活目标、embedding import 或 CAS 校验失败时，旧 active pointer 与状态保持不变。

### `0002 → 0003` 带数据升级

- migration 在 legacy assertions 与 bootstrap 前取得 `active_snapshot_pointers`、`corpus_snapshots` 的 `SHARE ROW EXCLUSIVE` 锁，阻止校验与约束安装之间发生 legacy 写入。
- 健康 legacy active pointer 被转换为 revision 1 的 `migration_bootstrap` event；bootstrap ID 对结构化 JSON 数组求 hash，scope/snapshot 中包含分隔符也不会产生字段边界歧义。
- 集成测试在独立临时 PostgreSQL 数据库先升级到 `0002_m3_immutable_rows`，构造两组旧拼接会碰撞的真实 active snapshot/pointer，再升级到 head，验证两个不同 event、pointer/event FK、timestamp 和 Alembic schema check。inconsistent legacy 数据继续失败关闭，不在 migration 中猜测或静默修复。

## 模块 D 核心语义

### 可复现 import plan、validate 与 apply

- `plan` 与其别名 `dry-run` 只读取调用方明确提供的本地 manifest、数据集、chunks、index manifest 和 embedding cache。所有引用路径必须是 `source-root` 内的安全相对路径；输入 schema、UTF-8/JSON、内容 hash、chunk 顺序、embedding profile、维度、归一化、模型 revision 和 artifact manifest 会在计划阶段统一验证。
- Plan 是 canonical、确定性、机器可读的内容身份，不保存 API key、数据库 URL、原始凭证或绝对私人路径。`validate` 会从当前本地产物重新构建计划并精确比较；产物发生变化或 plan 被修改时失败关闭，且两个命令都不导入数据库依赖、不解析数据库环境变量、不连接数据库。
- `apply` 在解析数据库设置或执行显式 `--migrate` 前再次完成本地验证，然后使用既有事务 importer 导入不可变行并从数据库回读 count/hash/dimension。生成的 receipt 绑定 plan、snapshot、profile、导入回执、迁移事实和回读事实；重复完全相同的 apply 幂等，冲突或漂移拒绝。导入不会自动激活快照。
- Plan 和 receipt 输出使用原子 no-clobber 发布，已有路径不会被覆盖。错误输出是稳定的机器 envelope，并对数据库连接信息和本地内容做最小披露。

### exact 默认与显式实验 HNSW

- 数据库 exact inner-product 仍是默认和权威路径。新 HNSW manager/repository 只有调用方显式选择时才使用，不会改变既有 exact retriever、离线 dense 或 BM25 默认行为。
- HNSW 物理索引按 snapshot、profile、dimension、参数和 embedding generation 隔离；`0004_m3_ann_build_guards` 引入 generation/build 约束。Build receipt 记录 canonical params、PostgreSQL/pgvector 版本、物理索引、profile manifest 和 generation，读取时会重算并通过 catalog/`EXPLAIN` 证明实际计划使用唯一匹配索引。
- Profile generation 一旦变化、receipt/物理索引缺失或冲突、snapshot/profile/import receipt 不再 serviceable，ANN 在查询前失败关闭。pgvector HNSW 大于 2000 维时在 DDL 前拒绝；相同 profile 的 exact 检索仍可工作。
- HNSW 使用与 exact 相同的 typed boundary 和 filter-before-limit SQL，不允许通过 ANN 路径绕过 scope、snapshot、profile、law/version/article 或 effective-date selector。

### typed underfill 与受控 exact fallback

- ANN 结果把 `ann_complete`、`ann_underfilled`、`exact_fallback_complete`、`exact_fallback_exhausted`、`exact_fallback_timed_out` 和 boundary deny-all 分成互斥状态；返回数量、ANN 初始数量、eligible population、fallback 使用情况和稳定原因均有强校验。
- 关闭 fallback 时，list-only adapter 不会把 partial ANN rows 当作完整 top-k 成功；调用方必须处理 typed underfill。
- 开启 fallback 时，只在完全相同的 typed boundary 内运行 exact，并设置独立 statement timeout。Exact 可以填满 top-k，或明确证明 eligible population 本来就小于 top-k；timeout 不会退回未经证明的 partial 成功。
- ANN 与 exact fallback 结果继续携带完整 `RetrievalProvenance`、content/vector/payload hash 和 trace，且稳定排序、重复 chunk 冲突与多 article 严格组合过滤仍适用。

### 迁移韧性、服务重启与累计门禁

- migration 自动回归覆盖 fresh 到 head、真实旧 revision 升级、`0003` activation 数据的安全 downgrade/re-upgrade、`0004` generation/build guard，以及不安全 legacy 状态的 fail-closed。Schema 和数据事实都在独立临时数据库验证，不以 mock 代替。
- `m3_restart_probe.py prepare` 在服务重启前持久化合成 snapshot、profile、exact/catalog/HNSW 事实与 receipt；CI 真实重启 PostgreSQL service container，等待 readiness 后由新 Python 进程执行 `verify`。验证覆盖 migration revision、pgvector/PostgreSQL 版本、导入和 activation 数据、exact、catalog、ANN build receipt 与返回结果。
- 累计 M3 gate 继承 M0-M2 的 25 项 mandatory checks，再加入 M3-T01 至 M3-T08 共 8 项，合计 33 项。M3-T08 除数据库测试外必须验证 restart receipt；缺少、伪造或不匹配 receipt 时不会标记通过。

## 当前 M3 强制验收覆盖

| Test ID | 计划要求 | 当前状态 | 当前证据与缺口 |
|---|---|---|---|
| M3-T01 | 数量、内容 hash、维度一致，重复导入不增行 | passed on implementation head | deterministic plan/validate/apply、count/hash/dimension 回读、首次/重复/并发导入、immutable conflict 与篡改拒绝均通过 |
| M3-T02 | pgvector exact 与原 dense 等价 | passed on implementation head | 同 query vector、snapshot/profile、候选集的 NumPy inner product 与 pgvector `<#>` 转正 score、顺序及稳定 tie 全部一致 |
| M3-T03 | 法名 + 条号命中正确版本，不存在时不编造 | passed on implementation head | 精确法名、规范化条号、可选版本和半开日期过滤通过；不存在不 fallback，多身份/多版本/重叠/未知有效期返回 typed state |
| M3-T04 | 所有召回/融合/回退路径都应用过滤 | passed on implementation head | exact、bounded BM25、RRF、adaptive direct/multi-plan/follow-up、rerank、chat、ANN 与 exact fallback 均保持 typed boundary/provenance |
| M3-T05 | ANN underfill 可解释或受控回退，不移除过滤 | passed on implementation head | 过滤 underfill、list-only 拒绝 partial、同边界 exact fill、eligible population 不足与 statement timeout 均有确定性状态 |
| M3-T06 | 超维/profile 不匹配提前报错 | passed on implementation head | 16000 维存储上限、2000 维 HNSW 上限、数据库 dimension trigger、query profile/revision/dimension/normalization 与 generation mismatch 全部提前拒绝 |
| M3-T07 | 新快照失败不影响旧快照，激活/回滚原子 | passed on implementation head | 导入后段失败整事务回滚；完整 CAS、并发单赢家、原子 activate/replace/rollback 和失败保留旧 active 均通过 |
| M3-T08 | 空库迁移、上一版升级、进程重启后检索 | passed on implementation head | fresh/legacy 升级、`0003` downgrade/re-upgrade、`0004`、真实 PostgreSQL service restart、新进程 exact/catalog/ANN 复核和 wheel 资源均通过 |

以上逐项历史记录精确绑定实现 head `cf2c7a82195b63786d77bfb0d5cea9799b18013b`，并由 run 36073478416 的 `33/33` 机器门禁支持；相同 M3-T01 至 M3-T08 随后已在最终 PR head `fff2a404...` 和 release target `9395c221...` 上再次累计通过，不再只是实现-head 推断。

## 模块 A + B 历史测试结果（保留）

### 本地

- 完整默认测试套件：`612 passed, 157 subtests passed`。
- M3 PostgreSQL integration：`16 passed`，覆盖 storage import 与 exact retrieval。
- M3-B targeted offline/contracts：`210 passed`。
- wheel：`60` entries；包含 `0001_m3_storage_baseline.py`、`0002_m3_immutable_corpus_rows.py`，以及新增 `embedding_contracts.py`、`retrieval_contracts.py`、`storage/retrieval.py` 等 retrieval modules。
- `uv lock --check`：passed。
- 新增/修改 Python 文件的 targeted Ruff format/check：passed。
- `git diff --check`：passed；无 whitespace error。
- 真实模型、远程 embedding、reranker、Judge 和付费 API 调用：0。

### GitHub Actions

- 精确实现 head：`4bc912b09931803d53b4ca3ffdc81d302ba65813`
- Workflow：[run 36045040845](https://github.com/1040942669/legal-rag-agent/actions/runs/36045040845)，结论 `success`
- `offline / Python 3.12.13`：success。该 job 继续执行现有 M2 cumulative offline gate，不冒充尚未建立的 M3 最终累计门禁。
- `m3 storage / PostgreSQL 18 + pgvector 0.8.6`：success。使用 digest-pinned `pgvector/pgvector:0.8.6-pg18`，完成 fresh migration、16 项 integration tests、wheel build 和 packaged migration resource check。
- quality-gate artifact digest：`sha256:77be3dd106aabf0999c1d84519451bdaff4b7a88c3a68c32421d682702aafdca`
- storage artifact digest：`sha256:4f06ece14c2b178552b2ccacd285aa7120d202dcf527ae0369b219eb6e5fe113`

本地验证使用 PostgreSQL 18.1 + pgvector 0.8.1，CI canonical service 使用 PostgreSQL 18 + pgvector 0.8.6；两类证据分别记录，不互相冒充。

## 模块 C 增量验收结果

### 本地与精确 head CI

- 精确实现提交：`1399e18bfca90422829364491ce43d668517d1c4`
- Workflow：[run 36058465682](https://github.com/1040942669/legal-rag-agent/actions/runs/36058465682)，`offline` 与 `m3 storage` 两个 job 均为 `success`。
- CI 与本地 offline：`653 passed, 157 subtests passed`；JUnit 汇总 `810 tests, 0 failures, 0 errors, 0 skipped`；M0-M2 mandatory `25/25`。这些仍是既有 M0-M2 累计门禁，不冒充尚未冻结的 M3 最终 gate。
- 真实 PostgreSQL integration：26 项全部通过；CI 2.55 秒，本地 3.52 秒。覆盖结构化法条查找、版本/日期/范围隔离、多 chunk membership、payload hash、active pointer、CAS 并发、替换/回滚、deferred consistency guard 和带 legacy active pointer 的真实升级。
- wheel：62 个 entries；`0001_m3_storage_baseline.py`、`0002_m3_immutable_corpus_rows.py`、`0003_m3_snapshot_activation.py` 三个 migration 资源 `3/3` 齐全。
- quality-gate artifact digest：`sha256:3426a7d43cfb3053103ea8bf4574338fc94bfd79e3e762680b1d6e4b9443b704`
- storage artifact digest：`sha256:d32a9d80760774bd0094b4681cc8a6516c13a58b33b611e1d8571dd041d361d3`
- 真实模型、远程 embedding、reranker、Judge 和任何付费 API 调用：0。

## 模块 D 与完整实现候选验收结果

### 本地实现 head

- 精确实现 head：`cf2c7a82195b63786d77bfb0d5cea9799b18013b`
- 完整离线套件：`671 passed, 157 subtests passed`。
- 真实 PostgreSQL 18.1 + pgvector 0.8.1 integration：`61 passed`。
- M0-M3 cumulative quality gate：`33/33`；覆盖继承的 25 项 M0-M2 checks 与 M3-T01 至 M3-T08。
- 真实服务重启验证：passed。重启前 receipt 与重启后新进程验证均成功，不以 Engine dispose/reconnect 冒充服务进程重启。
- Wheel：passed；打包资源包含 Alembic `0001`、`0002`、`0003`、`0004` 以及运行 migration 所需的 env/template。
- 真实模型、远程 embedding、reranker、Judge 和付费 API 调用：0。

### GitHub Actions 实现 head

- Workflow：[run 36073478416](https://github.com/1040942669/legal-rag-agent/actions/runs/36073478416)，结论 `success`，两个 job 均成功。
- `offline / Linux / Python 3.12.13`：`671 passed, 157 subtests passed`；JUnit `828 tests, 0 failures, 0 errors, 0 skipped`。
- `m3 storage / PostgreSQL 18 + pgvector 0.8.6`：`61 passed`；digest-pinned service 经过真实 container restart，并由新进程完成持久状态验证。
- M3 cumulative gate：`33/33`；gate JSON SHA-256：`4bb6d3cef8f1bc5e69d8e61cd70f413e64aaecb02eace1b6a492a4ba59476aad`。
- Quality artifact digest：`sha256:24b2c6ae741859bea2b472c5a9b59f9fb0f406bddaaa729dd57c1102bf050de4`。
- Storage artifact digest：`sha256:8d3c65df1f09044df2c34fa8668915d6249e99af6695b42a4f96bc0c4e97d2dd`。
- Wheel build 与 packaged migration resource check：passed。
- `ALLOW_LIVE_MODEL_CALLS=false`；真实或付费模型调用：0。

本地 PostgreSQL 18.1/pgvector 0.8.1 与 CI PostgreSQL 18/pgvector 0.8.6 是两套独立证据。前者证明本机可复现，后者是仓库 canonical Linux CI；二者结果一致，但不能互相替代或合并成一个环境声明。

### 最终候选提交前的本地快照复验

- 候选文档收口过程中的本地前置快照已通过 M3 integration gate：`33/33`，离线回归 `671 passed, 157 subtests passed`，JUnit `828/0/0/0`，gate JSON SHA-256 为 `40097c8df2489cd59e2262e842fb8a6aeb45cbaa3be14ec6e051cd952fb41ac0`。该结果是提交前证据，不冒充随后形成的精确 Git commit；最终权威证据仍是精确 PR head CI。
- 最终 README、CHANGELOG、版本元数据与代码组成的包输入快照离线构建出 `legal_rag_assistant-0.4.0-py3-none-any.whl`：309,061 bytes、66 entries、SHA-256 `cafe1b43cf6d30bb146e6aee54492eebebc9794dc29dcd0de715004e2b03a269`。
- 同一包输入快照离线构建出 `legal_rag_assistant-0.4.0.tar.gz`：419,532 bytes、109 entries、SHA-256 `7bb7a6b22b405c4ff4f01fad159ba4fd47322ca8a9da643a79bd45796cc0242c`。
- wheel 与 sdist 的版本 metadata 均为 `0.4.0`，禁止路径 0，Alembic migration `0001` 至 `0004` 为 4/4；全新 Python 3.12.13 venv 使用 `--no-index --no-deps` 安装 wheel 后，distribution version、`legal_rag.__version__`、模块来源、`legal-rag` entry point、`legal-rag --help` 和安装后 migration 资源均核验通过。
- 包审计在仓库外临时快照进行，构建前后工作区 patch 哈希相同；未联网安装、未补装依赖、未改变仓库。该证据证明待提交内容在本地可构建，但仍不替代提交后精确 PR head 的 GitHub Actions。

### 最终候选、release target 与远端发布证据

- 最终 PR head `fff2a4046d04e34664b374553070d9384eeec3c6` 的 [run 36076238447](https://github.com/1040942669/legal-rag-agent/actions/runs/36076238447)：两个 job success；offline `671 passed, 157 subtests passed`、JUnit 828/0/0/0；PostgreSQL 18 + pgvector 0.8.6 integration 61；M3 cumulative `33/33`；真实 restart/new-process verify 与 wheel 资源检查通过。
- release target `9395c221ea1fc9a9e869b3db04bd76910b77f5b0` 的 [master run 36076759673](https://github.com/1040942669/legal-rag-agent/actions/runs/36076759673)：相同测试口径全部成功，包版本 expected/installed/module 均为 `0.4.0`。
- PR artifacts：offline digest `sha256:e06b0403b01094ad3b1d37ffb413ee1832a3d46773779d5ff18a67270c7a8737`；storage digest `sha256:65fa484d464401dee7306e082ec84bfa44c7d5d60f8c01dd57b2ef6f8bfc2215`。
- master artifacts：offline digest `sha256:73e2d731cb20340153d70067caa000b288a06c20dcdec76c61537d802baee731`；storage digest `sha256:0824ea95d6e31d64f8903c3d67fea07717e637652f33c6d5aeb32e3175e409e1`。内部 gate/JUnit/restart 哈希见 `docs/refactor/receipts/M3.json`。
- annotated Tag object `1aa41823030681e17b7da70c27b50463d7d997b1` peeled 到 `9395c221...`。Release 于 `2026-09-25T00:22:49Z` 发布，`isDraft=false`、`isPrerelease=false`、附件 0；远端 Tag/Release 均已核验。

## 安全、隐私与兼容性检查

- [x] 数据库集成测试只允许 loopback host，且必须显式设置测试开关。
- [x] 每次 pytest invocation 使用新数据库，避免历史 schema 或残留行造成假绿。
- [x] 数据库 URL 在对象表示和可公开诊断中脱敏。
- [x] bound retrieval 不以可变 metadata 或 trace fingerprint 替代完整 typed boundary/provenance。
- [x] snapshot/profile/import receipt 无效时，在首次 provider call 前拒绝构造；query vector 不合约时在距离查询前拒绝。
- [x] 结构化法条查询不依赖 embedding profile；exact title/number/version/date 不命中时不做模糊、语义或跨版本兜底。
- [x] active pointer 的 snapshot/revision/activation CAS、不可变 event、状态切换和回滚在同一事务完成；并发失败不留下部分状态。
- [x] `0002 → 0003` legacy bootstrap 在独立临时数据库真实执行，迁移锁覆盖 assertions、copy 和约束安装。
- [x] `0003` downgrade/re-upgrade 与 `0004_m3_ann_build_guards` 在真实临时数据库执行；不安全 legacy/generation 状态失败关闭。
- [x] Import plan/validate 在数据库依赖和数据库配置解析前完成严格本地校验；source-root escape、敏感字段、artifact 漂移和输出覆盖均被拒绝。
- [x] Exact 保持默认；HNSW 必须显式启用并验证 snapshot/profile/dimension/generation-bound receipt，不支持维度不会破坏 exact 路径。
- [x] ANN underfill/exact fallback 继承完整 typed boundary，fallback timeout 和 eligible population 不足保留显式状态。
- [x] CI 真实重启 PostgreSQL service container，并在新进程中复核 schema、持久数据、activation、exact/catalog/ANN receipt 与结果。
- [x] M2 unbound artifact/hash chain 保持可读；带 bound marker 的 artifact 不允许降级成无 provenance 数据。
- [x] 未读取、暂存或上传 `.env`、私人语料、embedding cache、模型权重或本地 `.tmp` 数据库目录。
- [x] 未调用真实模型、远程 embedding、reranker、Judge 或任何未授权付费服务。
- [x] 未强推、未绕过 review/保护规则；`v0.4.0` 只在 release-target master CI 成功后创建一次，远端核验后没有移动。

## 对抗审查结果

模块 B、C 与 D 的审查和累计回归没有发现会阻止候选的 P0 或 P1。以下 P2/边界为非阻塞后续优化，不改变实现 head 的候选验收结论：

1. retriever 在构造时会于 encoder 调用前完成 snapshot/profile/import receipt preflight；若 snapshot 在构造后被 archive，当前单次 `retrieve()` 会先编码，再由 repository 的查询期 context 复验拒绝。可在编码前再次做轻量 serviceability preflight，减少 snapshot 状态切换后的无效 provider 计算；现有实现不会返回 archived snapshot 数据。
2. `0002` 的关系闭包 trigger 可能一次锁定多个父 snapshot。后续可在 `FOR UPDATE` 前按 `snapshot_id` 显式 `ORDER BY`，为并发跨父关系写入提供确定锁顺序并进一步降低死锁概率；当前管理路径的 advisory lock 与测试未显示正确性缺陷。
3. PostgreSQL exclusion constraint 可以进一步增强同一 `law_id` 已核验版本的关键有效期重叠证明。当前 lookup 已在请求日期遇到多个有效版本时返回 `needs_disambiguation / overlapping_validity`，不会随机选择，因此不是错误答案缺口。
4. deferred activation consistency trigger 当前从全量 pointer/event 历史查找不一致项；后续可按触发记录的 scope 限定扫描以降低历史增长后的提交成本，不改变现有事务正确性。
5. `0003` 对 inconsistent legacy active pointer 明确失败关闭；`0003` downgrade/re-upgrade 和 `0004` generation guard 已进入自动回归。仍可继续扩大来自更早历史版本的损坏组合，但不能在 migration 中猜测修复。
6. HNSW 当前是显式实验能力，只验证正确性、边界和 underfill/fallback 合同，没有在完整真实法律语料上校准 recall、build time、index size 与 P95；exact 因而继续是默认。
7. Import workflow 要求调用方准备严格 manifest 和既有 chunks/index/embedding artifact；仓库不包含私人语料、完整 cache 或生产数据库配置，也不会自动推断这些输入。

本轮同时复验了 negative inner product 方向、稳定 tie、filter-before-limit、多 article 严格边界、typed provenance、artifact 兼容、snapshot/profile/import receipt、vector/payload hash、immutable trigger、Hugging Face immutable revision 与 `trust_remote_code` 限制，以及 HNSW generation binding、typed underfill、exact fallback、可复现 import、真实 service restart 和 migration/wheel 资源。

## 当前发布判断

- 模块 A-D 判定：实现范围 accepted；所有实现提交已推送，精确实现 head 为 `cf2c7a82195b63786d77bfb0d5cea9799b18013b`。
- M3-T01 至 M3-T08：在实现 head、最终 PR head 与 release target 上全部通过；累计门禁均为 `33/33`。
- M3 当前判定：`released_receipt_pending`。软件已发布且远端核验，但回执尚未合并，不能提前写成完整 `released`。
- PR #13：已普通合并；最终 head `fff2a404...`，merge/release target `9395c221...`，候选与 master 两套 CI 均成功。
- Tag / Release / receipt：annotated `v0.4.0` 与 GitHub Release 已发布；机器回执 `docs/refactor/receipts/M3.json` 已创建，receipt PR 尚待建立和验证。
- Issue / Milestone：保持 open，直到 receipt PR 合并且 receipt master CI 成功；随后关闭并由 finalize PR 记录。
- M4：没有开始，必须等待新的明确授权。

## 发布回执收口顺序

M3 软件与 Release 已完成，后续只执行文档回执收口，不提前进入 M4：

1. 提交并推送 README、STATE、HANDOFF、M3 报告、回执索引和 `M3.json`，创建独立 receipt PR；回执提交不移动或替换 `v0.4.0`。
2. 在 receipt PR 初始 head 通过两个 CI job 后，回填 PR URL、候选 SHA、CI 与 artifact 证据，再次等待精确最终 head CI。
3. 正常合并 receipt PR，并核验 merge 后 master CI；此时才关闭 Issue #12，确认 Milestone 4 无其他 open issue后关闭 Milestone。
4. 创建 finalize PR，记录 receipt final head、merge/master CI、Issue/Milestone 关闭时间，把 STATE 改为 `released`；finalize PR 自身正常 CI/merge 后停止。

## 回滚

- 软件已经发布，但没有生产数据库部署。停止数据库实验可不选择 database profile 或 HNSW；原离线 CLI、exact 路径与 M2 unbound artifact 仍可使用。
- 测试数据库 migration rollback 只用于开发验证；未来生产回滚优先切回已验证 active snapshot，不能把代码 `git revert` 当成数据库恢复。
- HNSW 是可重建派生索引；回滚实验 ANN 应停止选择对应 build 并回到 exact，不应删除 corpus 或篡改已验证 receipt。Import plan/apply 从不隐式切换 active snapshot。
- 已发布的 `v0.3.0` 与 `v0.4.0` 都不移动、不复用；后续回执/finalize 提交只记录事实，不改软件 Tag。
