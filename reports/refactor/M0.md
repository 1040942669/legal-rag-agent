# M0 基线与门禁阶段验收报告

> 发布后已核验。软件 Tag 为 `v0.1.1`，精确指向通过 `master` CI 的 merge commit；本回执由 Tag 之后的独立文档提交记录，Tag 不为文档自引用而移动。

## 版本和范围

- Milestone / planned version：M0 / `v0.1.1`
- 实际分支：`codex/m0-baseline`
- 远端阅读基线：`4ed96ec921593ef81de61ce545513e3f1211b5db`（`origin/master`）
- 保留的既有代码提交：`66da8e3056f8287997088d9f503846fd88c9b038`（本地已完成的 Phase 4B）
- M0 审计提交：`3c5cd1f397791e26465ccd4f27840c434ea18db3`
- 已验证实现候选：`b3a2b4605a66c66f76ac6f199552c613e623f44d`
- 已验证冻结候选（含验收文档）：`98db5d72f146dce163314050a9968764de2d14e0`
- 最终 PR head：`e426424b892bedd3bc1c5c13a34e94e7f1496c2f`
- Release target / merge commit：`cb7e01982ca6fb95cebde1e5d707527fd36a250c`
- PR：[pull/2](https://github.com/1040942669/legal-rag-agent/pull/2)
- Issue / Milestone：[issues/1](https://github.com/1040942669/legal-rag-agent/issues/1) / [milestone/1](https://github.com/1040942669/legal-rag-agent/milestone/1)
- 目标行为：建立可审计、无真实模型或语料依赖的离线工程基线；为 PR 和 `master` 提供统一质量门禁；固定版本源并澄清历史结果与当前能力边界。
- 明确不做：M1 指标语义修改、数据库、API、LangGraph、Redis/Celery、新检索策略、真实语料复跑、Embedding/reranker 运行、在线或付费模型调用、生产部署。
- 与原方案差异 / ADR：工作分支名称按当前 Codex 约定使用 `codex/` 前缀；仓库没有保护规则或强制 review，因此仍通过 PR、精确 SHA CI 和 diff 审查完成合并，不直接推业务改动到 `master`。

## 实际改动

| 文件/模块 | 改动原因 | 对外行为变化 | 兼容性 |
|---|---|---|---|
| `docs/refactor/BASELINE_AUDIT.md` | 冻结真实仓库、远端、环境和修改前基线 | 新增可追溯审计记录 | 不改变运行时 |
| `scripts/quality_gate.py` | 建立单一、机器可读的 M0 门禁入口 | 支持 `M0/offline`，未知 stage/mode 非零退出，输出 JSON | 不删除原测试入口 |
| `tests/fixtures/synthetic/`、`tests/test_m0_smoke.py` | 让 CI 不依赖真实法律语料、Key 或网络 | 使用明确标注为虚构且非法律的文本，并在测试中禁止 socket | 不接触本地语料 |
| `tests/test_m0_cli.py`、`tests/test_quality_gate.py` | 固化 CLI、版本与失败语义 | `--help` 不再先加载 `.env`；门禁缺项、跳过或失败均不能误判通过 | 原 CLI 子命令保留 |
| `.github/workflows/quality-gate.yml` | 对 PR / `master` push 执行相同门禁 | 最小只读权限、固定 Action SHA、Python 3.12.13 和 uv 0.10.10；不注入模型密钥 | 不启用付费任务 |
| `pyproject.toml`、`legal_rag/__init__.py`、`uv.lock` | 消除双版本源漂移并准备发布 | distribution metadata 为版本权威，版本为 `0.1.1` | lock 只更新本包元数据，无依赖升级 |
| README、CHANGELOG、历史计划/评测/结果/ADR | 区分旧 Phase 与当前 M0-M7，并降格过度能力表述 | 明确语料时点、引用 rank 检查、拒答分母和 verifier 限制 | 保留历史数字与负面结论 |

## 验收结果

| Test ID | 实际命令 / 检查 | 环境 | mandatory | 状态 | 退出码 | 证据位置/摘要 |
|---|---|---|---|---|---|---|
| M0-T01 | `git status`、`git remote -v`、`git rev-parse`、GitHub API/CLI 只读核对 | Windows / GitHub | yes | passed | 0 | origin 为目标仓库；用户未跟踪文档、既有 Phase 4B 提交和被忽略本地资料均被保留；见 `BASELINE_AUDIT.md` |
| M0-T02 | 修改前 `uv run --frozen pytest -q` | Windows / Python 3.12.12 | yes | passed | 0 | `70 passed in 0.43s` |
| M0-T02 | `uv run --offline --frozen --no-sync python scripts/quality_gate.py --milestone M0 --mode offline` | Windows / Python 3.12.12 / uv 0.10.10 | yes | passed | 0 | 实现候选全量 `89 passed in 0.76s`；7/7 必需门禁通过 |
| M0-T03 | 门禁内 `pytest -q tests/test_m0_smoke.py` | Windows；无 Key、无真实语料；测试硬阻断 socket | yes | passed | 0 | `1 passed in 0.04s`；fixture 自身声明完全虚构且不是法律文本 |
| M0-T04 | 门禁内版本核对与 `python -m legal_rag.cli --help` | Windows / Python 3.12.12 | yes | passed | 0 | expected / installed / module 均为 `0.1.1`；CLI help 正常退出 |
| M0-T04 | `uv build --offline`；新临时 venv 中 `uv pip install --offline --no-deps <wheel>` 并导入核对版本 | Windows / Python 3.12.13 | supplemental | passed | 0 | sdist、wheel 构建成功；全新环境安装后版本为 `0.1.1` |
| M0-T05 | 最终 PR head 的 GitHub Actions `M0 Quality Gate` | Ubuntu 24.04 / Python 3.12.13 / uv 0.10.10 | yes | passed | 0 | `e426424...` 成功：[run 35428009054](https://github.com/1040942669/legal-rag-agent/actions/runs/35428009054) |
| M0-T05 | merge SHA 的 `master` push CI | Ubuntu 24.04 / Python 3.12.13 / uv 0.10.10 | yes | passed | 0 | `cb7e019...` 成功：[run 35428226839](https://github.com/1040942669/legal-rag-agent/actions/runs/35428226839) |
| M0-T06 | README、评测方案、历史执行计划、结果报告和 ADR 的交叉审查 | 人工 diff + Markdown 门禁 | yes | passed | 0 | 旧 Phase、当前 M0-M7、历史指标、未实测能力、verifier/refusal 限制已分开陈述 |
| M0-D01 | 包含验收文档后的本地统一门禁 | Windows / Python 3.12.12 / uv 0.10.10 | supplemental | passed | 0 | 全量 `89 passed in 0.72s`；7/7 必需检查通过 |
| M0-Q01 | 门禁 Markdown 相对链接检查 | Windows | yes | passed | 0 | 24 个 Git candidate Markdown 文件通过 |
| M0-Q02 | 门禁 STATE / manifest JSON 检查 | Windows | yes | passed | 0 | 2 个 JSON 文件解析与状态约束通过 |
| M0-Q03 | 门禁候选凭证形状扫描 + 人工文件清单/diff 审查 | Windows | yes | passed | 0 | 79 个 Git candidate 文本无高置信凭证形状；未读取被忽略文件；该扫描不构成无泄漏保证 |
| M0-N01 | `--milestone M9 --mode offline` 失败路径 | Windows | supplemental | passed | 2 | 输出可解析 failed JSON，进程与报告退出码均为 2 |
| M0-L01 | `uv lock --check`；`uv sync --check --offline --frozen` | Windows / uv 0.10.10 | supplemental | passed | 0 | lock 已解析；环境会保持不变 |
| M0-W01 | 使用 PyYAML 解析 `.github/workflows/quality-gate.yml` | Windows | supplemental | passed | 0 | YAML 可解析且包含离线 job steps |

- 已有回归测试是否运行：是；修改前 70 个测试和候选全量 89 个测试均真实运行。
- 真实外部组件是否使用：仅 GitHub 仓库、PR 和 Actions；没有模型、数据库、消息队列或生产服务。
- 模型/真实语料是否使用：否。没有调用在线/本地生成模型、Embedding、reranker 或 judge；没有读取完整本地语料内容。
- 未运行项目及原因：历史 203 部法律质量实验、30 条生成子集、真实 reranker A/B、Python 3.10/3.11 兼容矩阵均未运行；它们不属于 M0 离线工程门禁，且部分需要真实语料、GPU 或模型预算。
- CI 对应的准确 SHA / URL：最终 PR head `e426424b892bedd3bc1c5c13a34e94e7f1496c2f` / [run 35428009054](https://github.com/1040942669/legal-rag-agent/actions/runs/35428009054)；Release target `cb7e01982ca6fb95cebde1e5d707527fd36a250c` / [master run 35428226839](https://github.com/1040942669/legal-rag-agent/actions/runs/35428226839)。

## 实验与指标

| 指标 | 基线 | 当前 | 样本数/分母 | 缓存/模型/语料条件 | 结论 |
|---|---|---|---|---|---|
| 离线测试 | 70 passed | 89 passed | 全部收集测试 | 无真实模型/语料；uv lock 环境 | M0 新增 19 个测试，无回归失败 |
| M0 必需门禁 | N/A | 7/7 passed | 7 项 mandatory checks | Git candidate 文件 + 合成 fixture | 工程门禁可复现；不代表法律问答质量 |
| 检索/生成质量 | 历史报告值 | 未重跑 | N/A | 未加载历史语料/cache/模型 | 不作新版本质量改善声明 |

- manifest / 代码 hash / 数据 hash：代码由上述 commit SHA 固定；合成 fixture 纳入 Git；真实数据与模型未参与，因此没有伪造其 hash。
- 负结果与代价：原 CLI 在解析 `--help` 前加载 `.env`，已通过延迟加载修复；质量门禁增加一次完整 pytest 与一次 smoke 运行，CI 安装和执行有固定时间成本；高置信凭证扫描可能漏报，仍需人工 diff 审查。

## 隐私与变更审查

- [x] diff 已审查，无 key、私人内容、未授权大数据上传。
- [x] 原 CLI 与历史数据/报告保留或有迁移说明。
- [x] 新指标没有与旧口径混比。
- [x] 仅声称实际通过的能力。

本地 `.env`、完整语料、Embedding/cache、课程/简历/面试材料和生成产物保持忽略，未进入 candidate 文件集合；Key 只记录“本地已配置”，没有读取或输出值。

## 发布前判断

- 判定：软件 Release 已发布并远端核验；在本回执 PR 合入前，状态为 `released_receipt_pending`。回执合入后 M0 为 `released`。
- 阻塞：无软件发布阻塞；仅剩回执文档 PR/CI/合并，不得重复发版或移动 Tag。
- 已知限制：M0 只证明离线工程基线；规则 verifier 不证明 claim-source 语义支持或法律正确性；旧拒答指标会把普通样例自动记为 true；Python 声明为 `>=3.10`，本阶段只固定验证了 3.12 系列；仓库没有强制保护规则。
- 回滚：发布前可关闭 PR 而不触碰 `master`。发布后通过新的 revert PR 回滚合并提交；本版没有数据库、数据或后台任务迁移。已发布 Tag 不移动、不复用；如需修复发布新的后续版本。

## 发布后核验

- 实际 PR / Tag / Release / target SHA：[PR #2](https://github.com/1040942669/legal-rag-agent/pull/2) / `v0.1.1` / [Release](https://github.com/1040942669/legal-rag-agent/releases/tag/v0.1.1) / `cb7e01982ca6fb95cebde1e5d707527fd36a250c`。
- 远程 Tag：annotated tag object 为 `9072726ee99c3d13903069f78b747f02a85c8093`，peeled target 为 `cb7e01982ca6fb95cebde1e5d707527fd36a250c`，与 Release target 和成功的 `master` CI head 一致。
- Release：`isDraft=false`，`isPrerelease=false`，GitHub `publishedAt=2026-09-19T07:05:12Z`，无附件。
- 远程核验时间与方法：`2026-09-19T07:05:42Z`；使用 GitHub Release API/CLI、GitHub Actions run 元数据和 `git ls-remote` peeled ref 交叉核验。
- 发布回执文件：`docs/refactor/receipts/M0.json`；由独立文档 PR 提交，不移动 `v0.1.1`。

## 下一步

只完成回执文档 PR/CI/合并并关闭 M0 Milestone，然后停止。M1-M7 均保持 `not_started`，不自动开始 M1。
