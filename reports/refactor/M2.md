# M2 实验反馈周期与断点续评阶段验收报告

> 当前状态：`release_candidate_pending`。M2 实现和累计离线门禁已经完成，正在冻结 `0.3.0` 软件候选；PR #9 尚未合并，`v0.3.0` Tag、GitHub Release、release target 和发布回执均尚未创建。本文只记录已经真实发生的结果，发布后再由独立回执提交补齐远端终态。

## 版本和范围

- Milestone / planned version：M2 / `v0.3.0`
- 实际分支：`codex/m2-experiment-lifecycle`
- 基线 SHA：`00ec3ad5b193a7442e427486e8bfade2dbabd482`，即 M1 回执收尾后的 `origin/master`
- 当前已验证实现与门禁 SHA：`1fe3ddd7e7cf627114c21846de43be560dce2c98`
- 软件 PR：[PR #9](https://github.com/1040942669/legal-rag-agent/pull/9)，当前仍为 draft
- Issue / Milestone：[Issue #8](https://github.com/1040942669/legal-rag-agent/issues/8) / [Milestone 3](https://github.com/1040942669/legal-rag-agent/milestone/3)
- Tag / Release：尚未创建；不得把计划版本、候选包或草稿 PR 写成已发布版本
- 目标行为：把实验身份、阶段缓存、逐 case 持久化、断点恢复、并发隔离、provider 错误、只读聚合和显式生命周期命令连成一条可核验链路，让下游改动可以精确复用上游结果，并让 replay 保持零实际外部调用。
- 明确不做：真实法律质量提升声明、未授权在线或付费模型调用、完整 203 部法律语料复跑、PostgreSQL/pgvector、FastAPI、LangGraph、Redis/Celery、生产部署和 M3 以后的工作。
- 主要 ADR：只接受精确缓存键，不做跨用户语义答案缓存；case 先独立原子落盘，聚合器只读 artifact；replay 使用新 experiment ID 且禁止回退 fresh；真实模型调用默认关闭；源码工作区用于提供真实 Git 和实现指纹。

## 实际改动

| 文件/模块 | 机制 | 对外行为变化 | 边界 |
|---|---|---|---|
| `legal_rag/experiment_runtime.py` | canonical manifest、分阶段 cache key、不可变 attempt/complete store、resume inventory、checkpoint、通用 work-unit runner | fresh/cache/replay 来源、阶段耗时和实际/来源调用账本可分开核验；损坏和不兼容状态失败关闭 | 当前 owner 锁主要用于本地文件系统执行，不是分布式任务租约 |
| `legal_rag/experiment_adapter.py` | evaluation/chat 生产适配、`ConversationMemory` 导出恢复、阶段观察持久化、纯评分、provider policy | 中断后可从已确认 checkpoint 恢复；重试按 timeout/429/网络与参数/鉴权分类 | provider 仍需显式预算与配置；本次没有真实 provider 运行 |
| `legal_rag/experiment_aggregation.py` | 只读取已持久化 case artifact，验证 checksum/manifest/status 后计算分母并发布内容寻址 bundle | 不运行 retriever、assistant 或 provider；损坏、失败、终止、重试耗尽和未运行不会被补成成功 | 指标正确性由确定性 fixture 验证，不代表真实法律 gold 已复核 |
| `legal_rag/experiment_registry.py` | 冻结 dataset registry、模式与 subset 约束 | v3 与固定生成子集标记为 legacy regression；offline fixture 明确为虚构内容 | 没有把既有开发集改名为盲测集 |
| `legal_rag/experiment_lifecycle.py`、`legal_rag/cli.py` | `experiment plan/run/resume/aggregate/replay` | plan 只读；run 拒绝已有 ID；resume 重建当前事实；replay 新建 ID、只允许精确命中；实验命令不加载 `.env` | 当前直接执行后端为 provider-free BM25；生成模式未授权时失败关闭 |
| `legal_rag/chat.py`、`legal_rag/models.py`、`legal_rag/evaluation.py` | 会话和评测结果的精确序列化边界 | 真实 adapter 能保存并恢复消息、Trace、回答、验证和 Judge 状态 | 不改变 M1 对语义支持和法律正确性的保守定义 |
| `scripts/quality_gate.py`、`.github/workflows/quality-gate.yml` | 累计 M2 门禁与机器可读报告 | M0 7 项 + M1 10 项 + M2 8 项，共 25 个唯一必检 ID；CI 运行 M2 而非旧 M1 gate | CI 是应用级离线约束，不是 OS 级 air-gap |
| `tests/test_m2_*.py` | manifest/store/runner/adapter/aggregation/lifecycle 的确定性 fake、损坏与对抗路径 | M2-T01 至 M2-T08 均有具名 selector；skip/xfail/零测试不算成功 | 未使用完整法律语料、真实 embedding、reranker、生成模型或 Judge |
| README、CHANGELOG、`pyproject.toml`、`uv.lock` | 命令、边界、真实执行记录和 `0.3.0` 版本候选 | 版本源与 lock 一致；公开文档不把候选写成已发布 | 发布 URL、Tag 和 receipt 只能在真实发布后补录 |

## 强制验收结果

| Test ID | 实际检查 | mandatory | 状态 | 证据摘要 |
|---|---|---|---|---|
| M2-T01 | 精确 runtime replay + 生命周期 fresh/replay/aggregate | yes | passed | 2 passed；相同工具输出，replay 实际外部调用 0，聚合只读 |
| M2-T02 | 阶段 key 定向失效 + lifecycle contract invalidation | yes | passed | 2 passed；Prompt 改动不使 retrieval 失效，chunk/embedding/index 改动使相关上游失效 |
| M2-T03 | generic runner 中断续跑 + CLI run/resume | yes | passed | 2 passed；已完成 case 不重跑，已有 ID 不能再次 run |
| M2-T04 | corrupt/pending artifact + publication conflict | yes | passed | 2 passed；损坏 case 被报告且不进入评分，不可变发布冲突被拒绝 |
| M2-T05 | 并发 session work units | yes | passed | 1 passed；会话隔离、并发有界、结果稳定排序、无重复/损坏 |
| M2-T06 | origin timing/call ledger + aggregate provenance | yes | passed | 4 passed；fresh/cache/replay 的耗时、actual calls 与 source provenance 分开 |
| M2-T07 | store resume compatibility + lifecycle corpus drift | yes | passed | 4 passed；配置或语料事实不一致时在复用前拒绝 |
| M2-T08 | retrieval-only 分母/N/A + 失败状态 | yes | passed | 2 passed；无 gold 不进入分母，失败/终止/耗尽/未运行显式且不计质量成功 |

## 累计门禁与 CI

- 本地命令：`uv run --offline --frozen --no-sync python scripts/quality_gate.py --milestone M2 --mode offline --output .tmp/m2-quality-gate-local.json`
- 本地时间：`2026-09-22T04:05:11Z` 至 `2026-09-22T04:07:17Z`
- 本地结果：25/25 mandatory checks passed；完整套件 `548 passed, 157 subtests passed`；JUnit `705 tests, 0 failures, 0 errors, 0 skipped`
- 精确提交 CI：[run 35685720818](https://github.com/1040942669/legal-rag-agent/actions/runs/35685720818)，head `1fe3ddd7e7cf627114c21846de43be560dce2c98`，`offline / Python 3.12.13` 成功，门禁报告上传成功
- 环境：本地 Windows / Python 3.12.12 / uv 0.10.10；CI Ubuntu / Python 3.12.13 / uv 0.10.10
- 隔离事实：门禁子进程使用 sanitized environment，`LEGAL_RAG_DISABLE_DOTENV=1`、`ALLOW_LIVE_MODEL_CALLS=false`，本里程碑真实模型/付费 API 调用数为 0
- 最终候选要求：文档、版本和包证据冻结后，必须在最终 PR head 重新运行同一 M2 gate；当前绿灯不能替代后续最终 head 绿灯。

## 无模型真实执行记录

该记录不是 mock 的命令展示，而是实际从 CLI 创建、执行、回放并聚合后的本地 artifact 摘要；artifact 目录被 Git 忽略，没有上传私人语料或运行产物。

| 字段 | source | replay |
|---|---|---|
| experiment ID | `m2-offline-f5f6e40` | `m2-offline-f5f6e40-replay` |
| code commit | `f5f6e40c1d1219188069b625f6fa693b78dce588` | 同左 |
| dirty | false | false |
| manifest hash | `ad4ebfb99fc5aca280f022880bec66211b301ce3c17b8233140fbd0036f6c813` | `0d89650c5d8d2c4c9275697d1efd519b494608a745e1812472b3f0b92227c6e5` |
| case 结果 | 2/2 succeeded | 2/2 succeeded |
| cache mode | fresh source execution | `replay: 2` |
| actual external calls | 0 | 0 |

- 聚合键：`befeb112749971f15329921e84603b4fb48c220469a0136c204d5a30fe4c4c0d`
- bundle hash：`c848654fecf3ad6333c482ada39befd5e976ceb198cdc0934cbb4f23d64c3432`
- 结论：离线 BM25 的 exact replay 和 artifact-only aggregate 可真实运行；这不证明远程 provider、完整语料或法律回答质量。

## 包候选

- `pyproject.toml` 是唯一版本源，已准备为 `0.3.0`；`legal_rag.__version__` 从 distribution metadata 读取，`uv.lock` 只更新本项目版本。
- 构建命令：`uv build --offline --out-dir .tmp/m2-candidate-packages`，成功生成 wheel 与 sdist。
- wheel：`legal_rag_assistant-0.3.0-py3-none-any.whl`，217,926 bytes，SHA-256 `cada4f6ce06586167c3bbf743e109c145a8c5de7a8fc3cc622923520e42f8563`，45 个归档条目。
- sdist：`legal_rag_assistant-0.3.0.tar.gz`，309,954 bytes，SHA-256 `3d6a057939f0f4dff96307fd4bb3f09146b7aa92ba2b1bde74e34adefeb9b820`，81 个归档条目。
- wheel/sdist 中 `.env`、`.git`、`artifacts/`、`reports/`、`__pycache__` 等禁止路径均为 0。
- 在全新 Python 3.12.13 venv 中使用 `--no-deps` 安装 wheel，再从仓库根目录之外以 `-I` 导入；distribution 与 module 均为 `0.3.0`，模块来自该 venv 的 `site-packages`，console entry point 为 `legal_rag.cli:main`。

## 数据、指标与失败边界

- M2 复用既有 v3 120 条和固定 30 条子集的 registry 身份，但本次发布门禁没有读取或重跑完整本地法律语料；`offline` CLI 只使用 2 条明确虚构、非法律内容的 fixture。
- 没有生成新的真实 Hit@k、MRR、faithfulness、citation validity 或法律准确率，因此不与历史值比较，也不声称质量提升。
- 新增的指标价值是口径可核验：有 gold 的检索分母、无 gold/N/A、生成/验证/Judge 未执行、provider 失败、case 损坏和 replay 来源分别记录。
- replay 报告中的 source provenance 不等于本次真实调用；actual call ledger 为 0 才表示本次没有外部请求。
- 小样本 timing 只证明字段与起止边界，不作为稳定服务 P95 或生产性能承诺。

## 隐私与变更审查

- [x] Git candidate 范围的高置信凭证扫描通过，findings 0。
- [x] 被忽略的 `.env` 未读取、未输出、未暂存；实验命令显式禁止 dotenv 加载。
- [x] 本地完整语料、cache、模型、`.tmp` 与 `artifacts/experiments/` 未加入 Git。
- [x] 没有调用真实模型、Embedding、reranker、Judge 或付费服务。
- [x] 没有强推、admin 绕过、关闭 review 规则或移动既有 Tag。
- [x] 损坏、失败和未运行状态未被改写为成功；旧历史报告未覆盖。

## 发布判断

- 当前判定：`release_candidate_pending`，不是 `released`。
- 当前阻塞：没有代码或测试阻塞；剩余正常发布步骤为候选包验证、最终 head M2 gate/CI、PR 转 ready、按仓库规则合并、release target master CI、annotated `v0.3.0` Tag、GitHub Release、远端交叉核验和独立发布回执 PR。
- 仓库规则：不得强推、不得绕过 review 或保护规则。若当前仓库没有 required review，也只使用普通 GitHub 合并入口。
- 已知限制：生命周期 CLI 依赖 Git 源码工作区；直接执行只覆盖 provider-free BM25；没有跨进程任务队列或分布式锁；没有复跑完整语料/模型；Python 声明为 `>=3.10`，当前必需证据主要来自 3.12.x。

## 回滚

- 发布前：继续在 PR #9 修正，保持 artifact 不可变，不删除失败历史，不创建或移动 Tag。
- 发布后：通过新的 revert PR 回滚 M2 merge commit；可关闭缓存或使用旧评测入口，但不得把 replay 与 fresh 报告混合，也不得删除已经生成的实验 artifact 来美化结果。
- 已发布 Tag 不移动、不复用；任何修复使用新版本。
- 本里程碑没有数据库迁移、生产任务或服务部署，因此无数据回滚和运行中任务恢复动作。

## 发布后待回填

以下字段只有真实发生后才允许填写：最终 PR head 与 CI artifact digest、merge/release target SHA、master CI、annotated tag object 与 peeled target、GitHub Release URL/时间/状态、package hashes、receipt PR/merge/master CI、Issue/Milestone 关闭时间。回填完成前，`STATE.json` 必须保持 M2 非 released，`docs/refactor/receipts/M2.json` 不得提前创建。
